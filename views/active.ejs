<%- include ('header',{title:'Payyed - Active'}); %>
<div class="container" style="height: 100vh;">
    <div class="modal">
        <div class="modal-title">
            <span>
                <i class="fas fa-user"></i>
            </span>
            <h3>Active your account</h3>
        </div>
        <form action="" method="post">
            <div class="form-group">
                <input type="text" name="token" id="token" required>
            </div>
            <div class="form-group">
                <% if(errors.length > 0){ %>
                <%- `<p style="margin-bottom:10px; color:red">* ${errors[0].msg}</p>`  %>
                <%}%>
            </div>
            <div class="form-group mb-10">
                <a href="" class="resend" style="font-size: 14px; margin-top: 10px;">Resend code</a>
            </div>
            <div class="form-actions">
                <input type="submit" value="Confirm">
            </div>
        </form>
    </div>
</div>
<%- include ('footer'); %>

                <script>
                    $(function () {
                        $('.resend').click(function (e) {
                            e.preventDefault();
                            $('#token').val('');
                            $.get("/api/resend-code-register",
                                function (data, textStatus, jqXHR) {
                                    if (data == 1) {
                                        alert('Đã gửi lại mã');
                                    }
                                    else {
                                        alert('Đã xảy ra lỗi');
                                        $(location).attr('href', '/login');
                                    }
                                },
                            );

                        })
                    });
                </script>